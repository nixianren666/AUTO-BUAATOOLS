# BUAA Signin & Boya Automation Engineering Standards

本项目为北航统一身份认证（SSO）、日常晨午检自动打卡、博雅素养课程抢选及自动签到工具。所有在此代码库工作的 Agent 必须严格无条件遵守以下工程铁律：

## 1. “双重视角”对抗式审查（Dual-Persona Engineering）
- 严禁“编写完成、单点跑通即交付”的盲目乐观行为；
- 完成任何逻辑修改后，必须立即切换为“恶意挑错的审查工程师（Adversarial Auditor）”视角，对以下维度进行地毯式挑刺与边界穷举：
  1. **长期后台挂机维度**：如果用户运行数小时，内存缓存是否会与服务端真实状态脱节？
  2. **进程并发与单例维度**：如果用户重复双击启动，是否能被系统级互斥锁精准拦截并拉起已有前台，杜绝多实例冲突与托盘泛滥？
  3. **网络与认证波动维度**：如果网络闪断、CAS Ticket 过期或接口返回非 200 JSON，系统是否优雅重试并记录清晰排查日志？
  4. **异常路径容错**：关键函数调用不可因为单点异常导致主调度循环崩塌，必须具备稳健的 fallback 降级保护。

## 2. 级联影响全链路分析（Cascading Impact Analysis）
- 严禁“头痛医头、脚痛医脚”的单点打补丁模式；
- 修改任何一个数据结构、计算规则或逻辑字段时，必须向上与向下递归分析以下全部 6 个层级：
  1. `core/client.py` / `core/boya.py`：底座网络请求与字段原始解析；
  2. `core/*_scheduler.py`：核心后台轮询与抢选/签到触发判定；
  3. `server/app.py`：Web API 数据序列化与学期统计逻辑；
  4. `server/static/js/*.js`：前端异步请求、状态解构与容错回显；
  5. `server/static/index.html`：UI 布局、按钮状态自适应（如已签到/已签退/已完结隐藏）；
  6. **本地日志与通知**：个人日志记录是否准确、脱敏且具备上下文透明度。

## 3. 全平台平权与发布完整性（Cross-Platform Parity）
- 严禁“只管 Windows，遗漏 macOS/Linux”的片面开发；
- 每次发布新版本或修补重大 Bug 时，必须确保：
  1. **跨平台兼容性**：代码逻辑与系统托盘、自启动、开机引导在 macOS（Apple Silicon）与 Linux 下无语法/环境报错；
  2. **构建产物完整性**：GitHub Release 中必须同步提供 Windows 安装程序（Setup.exe）、Windows 绿色便携版（portable.exe）、macOS DMG 镜像、macOS Zip 包及最新源码包；
  3. **构建时效一致性**：所有平台产物的更新时间戳必须保持在同一次发版周期内，杜绝旧版本残留。

## 4. 绝对零隐私泄露铁律（Zero-Leak Privacy Protocol）
- 严禁将任何开发或测试人员的真实学号、姓名、密码哈希、动态 SSO Token、Cookie 写入仓库代码或测试样例中；
- 严禁将包含真实学号或个人测试信息的 UI 界面截图提交至 Git 跟踪树；
- 每次 `git push` 或更新 Release 前，必须执行全库敏感词（学号、姓名、测试密码、GitHub Token 等）与 Git Tree 对象深度取证扫描，确保敏感信息匹配数为 0。

## 5. 常驻后台守护与透明心跳机制（Daemon Integrity & Heartbeat）
- 任何面向后台长效挂机的调度器（如博雅抢课守护）：
  1. 严禁依赖启动时的静态内存缓存，必须设置周期性（如 60 秒）静默全量课池同步，确保及时捕获退课名额与新课发布；
  2. 必须具备透明的心跳汇报机制（如每 10 分钟汇报一次全校课池态势），杜绝假死静默；
  3. 必须在北航真实接口或严密的 Mock 边界下进行实测验证，确保网络发包准确无误且精准防重。

## 6. 教学教务校历严格对齐（Academic Calendar Scoping）
- 所有涉及“学期达标数”、“历史选课”、“本学期进度”的统计与渲染：
  1. 必须优先解析官方接口提供的校历元数据（`semesterStartDate` 与 `semesterEndDate`）；
  2. 必须严格将已选课程的开课/结课时间限制在当前学期区间内，坚决杜绝往届历史选课跨学期污染当前进度。

## 7. 深度优先工程准则（No-Compromise Engineering）
- 无论处理何种任务，始终坚持全面、深入、高强度的推演与排查；
- 绝不因节省算力或额度而在测试覆盖、边界推演或代码重构质量上做任何折中。
